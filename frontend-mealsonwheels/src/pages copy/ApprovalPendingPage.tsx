import React from 'react';

const ApprovalPendingPage: React.FC = () => {
  return (
    <div>
      <h2>Your account is pending admin approval.</h2>
      <p>Please wait for an admin to approve your account. You will be notified via email once approved.</p>
    </div>
  );
};

export default ApprovalPendingPage;
